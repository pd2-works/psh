package ink.pd2.shell.plugin;

import ink.pd2.shell.core.CommandEvent;

public class Command extends ink.pd2.shell.core.Command {
    public Command(String name, CommandEvent event) {
        super(name, event);
    }
}
